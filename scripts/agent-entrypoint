#!/usr/bin/env zsh
emulate -L zsh
set -euo pipefail

/usr/local/bin/agent-home-bootstrap

if (( $# == 0 )); then
  exec claude
fi

if [[ "$1" == -* ]]; then
  exec claude "$@"
fi

if [[ "$1" == "shell" ]]; then
  shift
  exec zsh "$@"
fi

exec "$@"
